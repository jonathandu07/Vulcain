<article
	class="article-subnavbar">
	{# Créer un produit #}
	<div>
		<a href="{{ path('app_user_controler_produit',{User_Pseudo: user.UserPseudo})}}">Créer un produit</a>
	</div>
	{# Créer un service #}
	<div>
		<a href="{{ path('app_user_controler_service',{User_Pseudo: user.UserPseudo})}}">Créer un service</a>
	</div>
	{# modifier profil #}
	<div>
		<a href="{{ path('app_user_controler_edit',{User_Pseudo: user.UserPseudo})}}">modifier profil</a>
	</div>
	{# Se déconnecter #}
	<div>
		<a href="{{ path('app_logout')}}">Se déconnecter</a>
	</div>
	{# Supprimer compte #}
	<div>
		<form method="POST" action="{{ path('app_user_controler_delete', {'User_Pseudo': user.UserPseudo}) }}" onsubmit="return confirm('Êtes-vous sùr ?')">
			<input type="hidden" name="_method" value="DELETE">
			<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
			<button class="delete">Se désinscrire</button>
		</form>
	</div>
</article>
